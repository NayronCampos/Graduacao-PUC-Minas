   /*
            Comandos de saída.
            variável de entrada
            template string
        */

            var nome = prompt("Send your name:");
            document.write(`Hello, ${nome}`);
            alert(`Hello, ${nome}`);
            console.log(`Hello, ${nome}`);